# Training Center Frontend

Frontend Vue 3 cho he thong quan ly trung tam dao tao.

## Chay local

```bash
npm install
npm run dev
```

Ung dung chay tai `http://localhost:5173`. Vite proxy cac request `/api` sang
API Gateway tai `http://localhost:5000`.
